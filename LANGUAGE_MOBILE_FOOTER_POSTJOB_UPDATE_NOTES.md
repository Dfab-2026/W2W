# Language + Mobile Footer + Post Job Fix

Changed only requested areas:

- Footer containment improved on mobile dashboards using `100dvh` dashboard shell.
- Header and footer stay fixed; only middle dashboard content scrolls.
- Post Job card now uses available middle height and scrolls inside the card on mobile, so the full card is visible.
- Mobile text wrapping improved globally for dashboard cards/forms/buttons to prevent single-line overflow.
- Language selector icon changed to a circular language icon.
- Language translation re-applies after UI changes/renders to cover more dashboard text, placeholders, titles and labels.

Everything else remains unchanged.
