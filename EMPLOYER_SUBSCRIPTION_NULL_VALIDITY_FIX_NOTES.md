# Employer Subscription Null + Validity Fix

Updated only the requested areas:

- Fixed EmployerProfile runtime crash when employer `me` or `extra` is temporarily null.
- Subscription card now shows remaining days and end date for both free trial and paid plans.
- Paid plan card keeps same visual style as the 3-month free trial card, with data changing by selected plan.
- Users can select/subscribe to another plan even when one plan is already active.
- Remaining app behavior and UI left unchanged.
