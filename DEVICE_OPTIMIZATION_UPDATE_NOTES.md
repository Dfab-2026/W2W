# Device Optimization Update

Updated only layout responsiveness and selfie button alignment:

- Sign-in page compacted for mobile and desktop.
- Role selection page compacted so all text/cards remain visible on small screens.
- Selfie verification popup made responsive.
- Selfie buttons are kept in one row: Start/Stop, Retake, Submit.
- Camera preview uses responsive height so buttons are not hidden.
- Existing flows, data logic, dashboards, subscriptions, verification, attendance, chat, notifications, Razorpay, and admin logic are unchanged.

Note: Build was not run in this environment because Next.js dependency is not installed here.
